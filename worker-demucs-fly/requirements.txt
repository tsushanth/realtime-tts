# CPU-only PyTorch build (see README.md "Backend choice" for why PyTorch, not ONNX).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0
torchaudio==2.11.0
demucs==4.1.0
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
numpy==2.4.6
scipy==1.17.1
soundfile==0.14.0

# dev/test-only, not needed to run the server:
requests==2.32.3   # verification/load_test.py
